<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ifmg.POO.searchb">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/new_icon_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SearchB">
        <activity android:name=".Alerta"></activity>
        <activity android:name=".Search" />
        <activity android:name=".Bible" />
        <activity android:name=".Reading" />
        <activity android:name=".Versiculos" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".NotificaitonReminder"/>


    </application>

</manifest>